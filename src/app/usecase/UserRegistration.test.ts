import { UserRegistration } from './UserRegistration';

describe('Successful user registration', () => {
  describe('Given a prospective user wants to register on the platform', () => {
    it('create a new user account with the following details', () => {});
    it('sends a verification email to "john.doe@example.com"', () => {});
    it('prompt the prospective user to check their email to complete registration', () => {});
  });
});
